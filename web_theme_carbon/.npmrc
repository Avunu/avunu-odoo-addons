# Every @carbon/* package ships an `ibmtelemetry` postinstall. This is a
# build-time-only dependency and has no business phoning home; a CI with
# egress filtering would fail `npm ci` on it.
#
# Install with:  IBM_TELEMETRY_DISABLED=true npm ci --ignore-scripts
ignore-scripts=true
